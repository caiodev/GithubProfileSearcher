buildscript {

    ext {

        //AndroidXCore
        androidXCore = '1.5.0-alpha02'

        //AppCompat
        appCompat = '1.3.0-alpha02'

        //Browser
        browser = "1.3.0-alpha05"

        //Code quality checking
        //Detekt
        detektVersion = '1.11.2'
        //Ktlint
        ktlintVersion = '0.38.0'

        //Coil
        coil = '1.0.0-rc1'

        //Gradle
        gradleVersion = '4.0.1'

        //Koin
        koin = '2.1.6'

        //Kotlin
        kotlin = '1.4.0'

        //Ktx
        ktxCore = '1.5.0-alpha02'

        //LeakCanary
        leakCanary = '2.4'

        //Lifecycle Architecture components
        arch = '2.3.0-alpha07'

        //Material design
        materialDesign = '1.3.0-alpha02'

        //OkHttp
        okHttp = '4.8.1'

        //Retrofit
        retrofit = '2.9.0'
        serializationConverter = '0.6.0'

        //Room
        room = '2.3.0-alpha02'

        //Serialization
        serialization = '1.0-M1-1.4.0-rc'

        //Timber
        timber = '4.7.1'

        //Views/ViewGroups
        //ConstraintLayout
        constraintLayout = '2.0.1'

        //RecyclerView
        recyclerView = "1.2.0-alpha05"

        //SwipeRefreshLayout
        swipeRefreshLayout = '1.2.0-alpha01'

        //Test dependency versions
        archTest = '2.1.0'
        espresso = '3.3.0'
        coroutinesTest = '1.3.9'
        jacocoVersion = '0.8.5'
        junitAndroidX = '1.1.2'
        junit5 = '5.7.0-RC1'
        testCore = '1.3.0'

        toolsPath = "$project.projectDir/tools/"
    }

    dependencies {
        classpath "com.android.tools.build:gradle:$gradleVersion"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin"
        classpath "org.jetbrains.kotlin:kotlin-serialization:$kotlin"
    }

    repositories {
        google()
        jcenter()
        mavenCentral()
    }
}

plugins {
    id "io.gitlab.arturbosch.detekt" version "$detektVersion"
}

allprojects {
    repositories {
        google()
        jcenter()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}